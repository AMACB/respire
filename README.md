# RESPIRE: High-Rate PIR on Small-Record Databases

## Quickstart

```
$ cargo run --release --bin <...>
```

Set the environment variable `RUST_LOG=info` to get more detailed information during execution.